# Done/To-Do:
Feedback from last time: 
- **Project Final Module Testing With IT/Network/CyberSec Professionals:**
	- Overall went well generally. Feedback: Maybe make note that not all linux distros use same syntax, the door analogy should come earlier with the diagram in the ipv4 section (done - its own slide), analogies are all good, I somehow explained something wrong that I never have before with traceroute and nslookup = routing protocols and dns server ugh, arp or nmap would be good for the broadcast domain section for participants to see a broadast message (ip a is not an example and needs to eb taken off the slides - which I caught as soon as I saw it), take TCP/UDP off the OSI diagram b/c layer 4 -> should have caught before
	- ~~Issue: like 10 minutes before the presentation, I saw that when rbneverting from static IPs back to DHCP it keeps both, so instead of having them change the /etc/network/interfaces file back to DHCP, I just rolled their VMs back to a previous snapshot I had during their DHCP config. A bit clunky but worked and will fix the issue~~
	- Overall setup is cool, layer 3 diagram is good but should come earlier
	- I was a bit nervous but the feedback was super valuable - thanks to Will, Kendai, Mike, John
	- Stop at each lesson for hands on interactive part, like more in depth if we can
		- More often interactive also in stuff just like asking them stuff
	- Road route analogy for traceroute, each turn onto a different highway is like a traceroute router swap
	- termonlogy 
	- ip address hadns-on exercise:
		- instead of doing it as VMs, use unmanaged switch plugged into the ports we assign their static IPs on their local network so they can ping each other
		- Then, do with two unmanaged switches and compare that people can ping each other on one side, but now can plug an ethernet between the two unamanged switches and then the two sides can ping each other
		- Get a router like a GLiNet; wan, lan, dhcp server running on it. Connect box to the unmanaged switch system so they get dhcp
		- may be worth just embracing setting up their different systems and OS since they'll need to
		- Set two static IPs the same so they can see the conflict; could work with VMs too
		- Can do same with MAC addresses
		- Can do DHCP section after this
		- Maybe have at the start of the ipv4 section
- Have slide of simple all-OSI and something similar to the wiki table
- Have them do like nothing on proxmox if possible -> just use WSSH reconnect when changing dhcp as a teachable moment
- **presentation will be a bit different if the whole 3-4 hrs given which is good**
	- Also the backups won't complete due to storage limits
- Practice end-of-summer presentation
- Re-set up the PTG once I had to move it back again
- Threw broadcast domain image earlier in presentation.

**Solution for DHCP and static same:**
- after setting back to DHCP from static:
	- ip addr flush dev ens18 *(or whatever name for nic)*
	- systemctl restart networking

**Solution for backups not switching**
- For each node:
- get into the backups > dump
- rm vzdump-qemu-ASTERISK-2026_07_1ASTERISK and variations with the second asterisk let us change the days we remove
- setting up retention settings for ceph backups to clear automatically after certain times
- nsf process had it locked
- clear afterwards, then restart nfs